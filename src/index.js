import '../src/style/index.scss'
import framework from '../src/framework'
import domready from 'domready'
// import createStats from './util/stats'

// if (process.env.NODE_ENV !== 'production') {
//   createStats()
// }

domready(framework)
